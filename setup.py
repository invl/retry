from setuptools import setup


setup(
    name='retry',
    version='0.2.0',
    description='retry decorator',
    py_modules=['retry'],
    install_requires=[
        'decorator',
    ]
)
