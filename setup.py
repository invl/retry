from setuptools import setup


setup(
    name='retry',
    version='0.3.0',
    description='retry decorator',
    packages=['retry'],
    install_requires=[
        'decorator',
    ]
)
